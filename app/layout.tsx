import type { Metadata } from "next";
import "maplibre-gl/dist/maplibre-gl.css";
import "./globals.css";

export const metadata: Metadata = {
  title: "NYC 311 Priority Atlas",
  description:
    "Explore historical complaint demand, recorded administrative closure, neighborhood context, selection scenarios, and workload dynamics across New York City census tracts.",
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
